# smartinsuredreserve — Autonomous Build Backlog

SmartInsuredReserve is a Next.js 14 / React 18 / TypeScript single-page insurance deductible optimizer that projects a self-insurance reserve fund; prior phases shipped the projection/validation/scenario engine, the security & observability hardening (dead `Function()` removed, CSP `unsafe-eval` dropped, HSTS + edge `_headers`, `src/lib/observability.ts` with a `source` field, `MAX_CAPTURED_ERRORS`, `getCapturedErrors`, and capture on the `ReportActions` export path), and the `next.config.js`/`_headers` security-header parity. This phase deepens the derived-data layer on top of `src/lib/projection.ts`: new pure analytics modules (reserve metrics, reserve-health classification, scenario comparison, milestone detection), a detailed CSV export, and two additive insight builders — all additive so existing tests keep passing. Test command: `npm test`.

## Build
<!-- parallel -->
- [x] Create `src/lib/metrics.ts` exporting `computeReserveMetrics(rows: ProjectionRow[]): { totalReserveEarnings: number; peakRecapturedPremium: number; averageAnnualSavings: number }` (importing `ProjectionRow` from `./types`) where `totalReserveEarnings` is the sum of every `row.reserveEarnings`, `peakRecapturedPremium` is the maximum `row.recapturedPremium` (0 for an empty array), and `averageAnnualSavings` is the last row's `cumulativeSavings` divided by `rows.length` (0 for an empty array); add `tests/metrics.test.ts` asserting that for rows `[{ reserveEarnings: 10, recapturedPremium: 5, cumulativeSavings: 5 }, { reserveEarnings: 20, recapturedPremium: 8, cumulativeSavings: 13 }]` cast to `ProjectionRow` the result equals `{ totalReserveEarnings: 30, peakRecapturedPremium: 8, averageAnnualSavings: 6.5 }`, and that `computeReserveMetrics([])` equals `{ totalReserveEarnings: 0, peakRecapturedPremium: 0, averageAnnualSavings: 0 }`. Done when `npm test` exits 0.
- [x] Create `src/lib/reserveHealth.ts` exporting `classifyReserveHealth(coverageRatio: number): 'under' | 'adequate' | 'strong'` that returns `'under'` when `coverageRatio < 1`, `'adequate'` when `coverageRatio >= 1 && coverageRatio < 2`, and `'strong'` when `coverageRatio >= 2`; add `tests/reserveHealth.test.ts` asserting `classifyReserveHealth(0.5) === 'under'`, `classifyReserveHealth(1) === 'adequate'`, `classifyReserveHealth(1.9) === 'adequate'`, and `classifyReserveHealth(2) === 'strong'`. Done when `npm test` exits 0.
- [x] Create `src/lib/scenarioCompare.ts` exporting `compareProjections(base: ProjectionRow[], alt: ProjectionRow[]): { finalReserveDelta: number; totalSavingsDelta: number }` (importing `ProjectionRow` from `./types`) where `finalReserveDelta` is the last `alt` row's `smartInsuredReserve` minus the last `base` row's `smartInsuredReserve` (each missing final row treated as 0) and `totalSavingsDelta` is the last `alt` row's `cumulativeSavings` minus the last `base` row's `cumulativeSavings` (missing treated as 0); add `tests/scenarioCompare.test.ts` asserting that comparing base `[{ smartInsuredReserve: 1000, cumulativeSavings: 200 }]` against alt `[{ smartInsuredReserve: 1500, cumulativeSavings: 350 }]` (cast to `ProjectionRow`) yields `{ finalReserveDelta: 500, totalSavingsDelta: 150 }`, and that `compareProjections([], [])` yields `{ finalReserveDelta: 0, totalSavingsDelta: 0 }`. Done when `npm test` exits 0.
- [x] Create `src/lib/reserveMilestones.ts` exporting `findReserveMilestones(rows: ProjectionRow[], targets: number[]): Record<number, number | null>` (importing `ProjectionRow` from `./types`) that, for each target, maps it to the `year` of the first row whose `smartInsuredReserve >= target` or `null` when no row reaches it; add `tests/reserveMilestones.test.ts` asserting that for rows `[{ year: 1, smartInsuredReserve: 500 }, { year: 2, smartInsuredReserve: 1500 }, { year: 3, smartInsuredReserve: 3000 }]` (cast to `ProjectionRow`) and targets `[1000, 5000]` the result equals `{ 1000: 2, 5000: null }`. Done when `npm test` exits 0.
- [x] Update `src/lib/csv.ts` to add and export `buildDetailedProjectionCsv(rows: ProjectionRow[]): string` whose first line is exactly `Year,Auto Deductible,Home Deductible,Auto Premium,Home Premium,Proposed Premium,Current Premium,Recaptured Premium,Beginning Reserve,Reserve Earnings,Reserve Balance,Cumulative Savings` and whose data lines join `year,autoDeductible,homeDeductible,autoCurrentPremium,homeCurrentPremium,proposedPremium,currentPremium,recapturedPremium,beginningReserve,reserveEarnings,smartInsuredReserve,cumulativeSavings` with commas, leaving the existing `buildProjectionCsv` export and its header unchanged so `tests/csv.test.ts` still passes; add `tests/csv-detailed.test.ts` asserting the first line of `buildDetailedProjectionCsv([oneRow])` equals that exact detailed header, that `buildDetailedProjectionCsv([])` equals just the header line, and that two rows produce exactly three newline-separated lines. Done when `npm test` exits 0.
## Build (continued)
- [x] Update `src/lib/insights.ts` to add and export `deriveMetricInsights(rows: ProjectionRow[], inputs: ProjectionInputs): string[]` that calls `computeReserveMetrics` from `./metrics` and `formatCurrency` from `./format` and returns exactly three strings `Total reserve earnings: <formatCurrency(totalReserveEarnings)>`, `Peak annual savings: <formatCurrency(peakRecapturedPremium)>`, and `Average annual savings: <formatCurrency(averageAnnualSavings)>`, leaving the existing `deriveInsights` export unchanged so `tests/insights.test.ts` still passes; add `tests/insights-metrics.test.ts` asserting `deriveMetricInsights` for rows `[{ reserveEarnings: 100, recapturedPremium: 250, cumulativeSavings: 250 }]` (cast to `ProjectionRow`) returns an array of length 3 whose first element includes `Total reserve earnings:` and whose text contains `$100`. Done when `npm test` exits 0.
- [x] Update `src/lib/insights.ts` to add and export `deriveHealthInsight(rows: ProjectionRow[], inputs: ProjectionInputs): string` that calls `computeSummaryStats` from `./summary` and `classifyReserveHealth` from `./reserveHealth` and returns a string of the form `Reserve health: <classification>` where `classification` is derived from `stats.reserveCoverageRatio`, leaving `deriveInsights` and `deriveMetricInsights` unchanged so `tests/insights.test.ts` and `tests/insights-metrics.test.ts` still pass; add `tests/insights-health.test.ts` asserting that for a single row with `smartInsuredReserve: 12000` (cast to `ProjectionRow`) and inputs `{ autoCurrentDeductible: 500, homeCurrentDeductible: 1000 }` (cast to `ProjectionInputs`) the returned string includes the substring `strong`, and for a single row with `smartInsuredReserve: 2000` it includes the substring `under`. Done when `npm test` exits 0.
- [ ] Create `src/lib/reportSummary.ts` exporting `buildReportSummary(rows: ProjectionRow[], inputs: ProjectionInputs): { metrics: { totalReserveEarnings: number; peakRecapturedPremium: number; averageAnnualSavings: number }; health: 'under' | 'adequate' | 'strong'; insights: string[] }` that composes `computeReserveMetrics` from `./metrics`, `classifyReserveHealth` from `./reserveHealth` applied to `computeSummaryStats(rows, inputs).reserveCoverageRatio` from `./summary`, and `deriveInsights` from `./insights`; add `tests/reportSummary.test.ts` asserting that for a single row with `smartInsuredReserve: 12000, cumulativeSavings: 400, reserveEarnings: 50, recapturedPremium: 400` (cast to `ProjectionRow`) and inputs `{ autoCurrentDeductible: 500, homeCurrentDeductible: 1000 }` (cast to `ProjectionInputs`) the result's `metrics.totalReserveEarnings === 50`, `result.health === 'strong'`, and `Array.isArray(result.insights)` with `result.insights.length >= 3`. Done when `npm test` exits 0.
