# smartinsuredreserve — Autonomous Build Backlog
SmartInsuredReserve is a Next.js 14 / React 18 / TypeScript single-page insurance deductible optimizer that projects a self-insurance reserve fund; prior phases shipped the projection engine, input validation, scenario save/load, CSV/print export, reduced-motion handling, SEO/social/canonical metadata, JSON-LD, a hydration skeleton, Dependabot, a tightened CSP, production source maps, the centralized `src/lib/defaults.ts` / `src/lib/deductibleTiers.ts` / `src/lib/messages.ts` modules, and Phase 6's `src/lib/insights.ts`, `SelectField`, and `InsightsPanel` wired into the optimizer — all merged and passing. Phase 7 deepens the derived-reserve summary and insights (surfacing average annual savings, reserve growth, and coverage ratio), gives the insights panel a real heading, finishes the display-formatting DRY paydown by routing the optimizer's stat cards and chart tooltip through `formatCurrency`, and adds a summary CSV export. Test command: `npm test`.

## Build
- [ ] Update `src/lib/summary.ts` to add two fields to the `SummaryStats` interface and its returned object — `averageAnnualSavings: number` equal to `totalSavings / rows.length` (or `0` when `rows` is empty) and `reserveGrowth: number` equal to `finalReserve - inputs.currentReserve` — leaving every existing field and all `tests/summary.test.ts` assertions unchanged; add `tests/summary-enrichment.test.ts` that calls `computeSummaryStats` with two `ProjectionRow` objects whose final row has `cumulativeSavings` 1000 and `smartInsuredReserve` 2000 and `inputs.currentReserve` 500 and asserts `averageAnnualSavings` equals 500 and `reserveGrowth` equals 1500, and asserts `computeSummaryStats([], inputs).averageAnnualSavings` equals 0. Done when `npm test` exits 0.
- [ ] Update `src/lib/insights.ts` so `deriveInsights` appends three lines derived from `computeSummaryStats` — an average-annual-savings line containing `formatCurrency(stats.averageAnnualSavings)`, a reserve-growth line containing `formatCurrency(stats.reserveGrowth)`, and a coverage-ratio line containing `formatPercent(stats.reserveCoverageRatio * 100)` (import `formatPercent` from `src/lib/format.ts`) — returning at least six non-empty strings and keeping `tests/insights.test.ts` passing; add `tests/insights-enrichment.test.ts` that calls `deriveInsights` with two rows whose final row has `smartInsuredReserve` 20000 and `cumulativeSavings` 4000 and asserts the result has length at least 6, that at least one element includes the substring '$2,000', and that at least one element includes the substring '%'. Done when `npm test` exits 0.
## Build (continued)
<!-- parallel -->
- [ ] Update `src/components/InsightsPanel.tsx` to render an `<h2 id="reserve-insights-heading">Reserve insights</h2>` as the first child of the `<section>` and change the section from `aria-label="Reserve insights"` to `aria-labelledby="reserve-insights-heading"`, keeping the `<ul>`/`<li>` mapping over `deriveInsights(rows, inputs)` intact so `tests/InsightsPanel.test.tsx` still passes; add `tests/insights-panel-heading.test.tsx` (with an `@jest-environment jsdom` docblock) that renders `InsightsPanel` with a minimal `inputs` object and a rows array whose final row has `smartInsuredReserve` 12345 and asserts an element with role `heading` and accessible name matching /reserve insights/i is present. Done when `npm test` exits 0.
- [ ] Update `src/components/InsuranceOptimizer.tsx` to import `formatCurrency` from `src/lib/format.ts` and render the four summary stat-card values (Final Reserve Balance, Total Savings, Final Auto Deductible, Final Home Deductible) and the Recharts `<Tooltip>` formatter via `formatCurrency(...)`, removing every `.toLocaleString(` call and the now-redundant literal `$` prefixes on those stat cards, while preserving the `SelectField`, `InsightsPanel`, `DEFAULT_PROJECTION_INPUTS`, and `FirstUseNotice` usages, the `./SelectField` import, and every `aria-hidden="true"` attribute so `tests/optimizer-wiring.test.ts`, `tests/optimizer-insights-source.test.ts`, `tests/optimizer-select-source.test.ts`, and `tests/icon-accessibility.test.ts` still pass; add `tests/optimizer-format-source.test.ts` that reads `src/components/InsuranceOptimizer.tsx` with `fs.readFileSync` and asserts the source contains `formatCurrency` and the substring '../lib/format' and does NOT contain the substring 'toLocaleString'. Done when `npm test` exits 0.
- [ ] Update `src/lib/csv.ts` to add and export a pure function `buildSummaryCsv(rows: ProjectionRow[], inputs: ProjectionInputs): string` (types imported from `src/lib/types.ts`) that calls `computeSummaryStats` from `src/lib/summary.ts` and `formatCurrency` from `src/lib/format.ts` and returns a CSV string beginning with the header line 'Metric,Value' followed by rows for `Final Reserve`, `Total Savings`, and `Break-Even Year`, leaving `buildProjectionCsv` and `tests/csv.test.ts` unchanged; add `tests/summary-csv.test.ts` that calls `buildSummaryCsv` with a minimal `inputs` object and one row whose `smartInsuredReserve` is 5000 and asserts the output starts with the substring 'Metric,Value', contains the substring 'Final Reserve', and contains the substring '$5,000'. Done when `npm test` exits 0.
