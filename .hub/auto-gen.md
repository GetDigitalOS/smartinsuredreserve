# smartinsuredreserve — Autonomous Build Backlog

SmartInsuredReserve is a Next.js 14 / React 18 / TypeScript single-page insurance-deductible reserve calculator whose projection engine, domain helpers, input validation, scenario persistence, and presentational components (`SummaryStats`, `ProjectionTable`, `ScenarioPanel`) were extracted, typed, and unit-tested in Phase 1 (merged to main). Phase 2 closes the outstanding compliance gaps: dependency vulnerabilities (4 reported — 2 high, 2 moderate), an over-permissive CSP (`unsafe-eval`/`unsafe-inline`), missing client-side error capture/observability, and security headers that do not apply on the `cloudflare-pages` static deploy target. The done-gate for every task is the test command `npm test`.

## Build

- [ ] **Pin patched dependencies in `package.json`**: Bump `dependencies.next` to `^14.2.0` (or higher patched minor) and add an `overrides` block setting `postcss` to `>=8.4.31` and `brace-expansion` to `>=2.0.2` to resolve the PostCSS XSS and brace-expansion DoS advisories; add `tests/dependencies.test.ts` reading `package.json` and asserting `pkg.dependencies.next` matches `/^\^?14\.(2|[3-9])/`, `pkg.overrides.postcss` is defined and starts with `>=8.4.3`, and `pkg.overrides['brace-expansion']` is defined. Done when `npm test` exits 0.

- [ ] **Tighten CSP in `next.config.js`**: Edit the `Content-Security-Policy` header value (around line 27) to remove `'unsafe-eval'` and `'unsafe-inline'` from `script-src` (safe now that the `Function()`/`evaluateExpression` helper was deleted in Phase 1), keeping `default-src 'self'`; add `tests/csp.test.ts` that `require`s `next.config.js`, resolves the `headers()` array, locates the `Content-Security-Policy` value, and asserts the `script-src` directive contains `'self'` but does NOT contain `'unsafe-eval'` and does NOT contain `'unsafe-inline'`. Done when `npm test` exits 0.

- [ ] **Guard against dynamic code execution in `tests/no-eval.test.ts`**: Add `tests/no-eval.test.ts` that reads the source text of `src/lib/calculator.ts`, `src/lib/projection.ts`, and `src/lib/scenarios.ts` and asserts none contain the substrings `Function(`, `eval(`, or `new Function` — locking in the removal of the scaffold expression evaluator that previously forced `'unsafe-eval'` in the CSP. Done when `npm test` exits 0.

- [ ] **Static security headers for Cloudflare Pages in `public/_headers`**: Create `public/_headers` with a `/*` route block emitting `X-Frame-Options: DENY`, `X-Content-Type-Options: nosniff`, `Referrer-Policy: strict-origin-when-cross-origin`, `Permissions-Policy: geolocation=(), microphone=(), camera=()`, and a `Content-Security-Policy` line whose `script-src` is `'self'` only (no `unsafe-eval`/`unsafe-inline`) so headers apply on the static deploy where `next.config.js headers()` does not run; add `tests/headers.test.ts` reading `public/_headers` and asserting it contains `X-Frame-Options: DENY`, `X-Content-Type-Options: nosniff`, a `Content-Security-Policy:` line, and that the CSP line does not include `unsafe-eval`. Done when `npm test` exits 0.

- [ ] **Observability module `src/lib/observability.ts`**: Implement `captureError(error: Error, context?: Record<string, unknown>): void`, `captureMessage(message: string, level?: 'info' | 'warning' | 'error'): void`, and `getCapturedEvents(): { kind: string; message: string; level: string }[]` backed by a module-level in-memory buffer (noop transport when no DSN is configured) so Tier 2 error capture exists without a network dependency; add `tests/observability.test.ts` asserting `captureError(new Error('boom'))` pushes an event whose `message` is `'boom'` and `level` is `'error'`, `captureMessage('hi')` records `level` `'info'` by default, and `getCapturedEvents()` returns both in order. Done when `npm test` exits 0.

- [ ] **Global error handlers in `src/lib/observability.ts` wired via `InsuranceOptimizer.tsx`**: Add `initObservability(): void` to `src/lib/observability.ts` that idempotently attaches `window` `'error'` and `'unhandledrejection'` listeners forwarding to `captureError`, and call `initObservability()` from a `useEffect(() => { initObservability() }, [])` in `InsuranceOptimizer.tsx` (verify wiring with `grep -rn "initObservability" src/components/InsuranceOptimizer.tsx`); extend `tests/observability.test.ts` (or add `tests/observability-handlers.test.ts`) asserting that after `initObservability()` dispatching `new ErrorEvent('error', { error: new Error('global'), message: 'global' })` on `window` records an event via `getCapturedEvents()`, and that calling `initObservability()` twice does not register duplicate handlers (event recorded once). Done when `npm test` exits 0.

- [ ] **Error boundary `src/components/ErrorBoundary.tsx` wrapping the app**: Implement an `ErrorBoundary` React class component with `getDerivedStateFromError` and `componentDidCatch(error, info)` calling `captureError` from `src/lib/observability.ts`, rendering fallback text `Something went wrong` plus a `Reset` button (clearing error state) when a child throws and rendering `children` otherwise, then wrap the JSX returned by `InsuranceOptimizer.tsx` in `<ErrorBoundary>...</ErrorBoundary>` (verify with `grep -rn "ErrorBoundary" src/components/InsuranceOptimizer.tsx`); add `tests/ErrorBoundary.test.tsx` using `@testing-library/react` asserting it renders a normal child when no error, renders `Something went wrong` when a child component throws, and that `captureError` is invoked once on catch. Done when `npm test` exits 0.