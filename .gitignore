node_modules/
.next/
.env
.DS_Store

# Claude Code
.claude/settings.local.json

# Build-loop ephemeral scratch — written to the tree, deleted by the loop,
# never read back from a commit. Tracking these resurrects them (ADR-034).
.hub/spec-draft.md
.hub/auto-gen.md
.hub/spec-doctor-proposal.json
.hub/agy-prompt.md

# Build caches — regenerated on every build, never meaningful to track;
# tracking them makes every branch diverge and every loop rebase collide (ADR-052).
*.tsbuildinfo
.turbo/
coverage/
